import { CalcCreditPipe } from './calc-credit.pipe';

describe('CalcCreditPipe', () => {
  it('create an instance', () => {
    const pipe = new CalcCreditPipe();
    expect(pipe).toBeTruthy();
  });
});
